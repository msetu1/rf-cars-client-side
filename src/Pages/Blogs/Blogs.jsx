import CommonBanner from "../../Ui/CommonBanner/CommonBanner";

const Blogs = () => {
    return (
        <div>
             <CommonBanner bannerText={'Blogs'} bannerBtn={`Blogs`}/>
        </div>
    );
};

export default Blogs;