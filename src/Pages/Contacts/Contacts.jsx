import CommonBanner from "../../Ui/CommonBanner/CommonBanner";

const Contacts = () => {
    return (
        <div>
             <CommonBanner bannerText={'Contacts'} bannerBtn={`Contacts`}/>
             
        </div>
    );
};

export default Contacts;